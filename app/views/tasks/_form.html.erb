<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= @task.list.name %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <% supp = "supp".to_sym %>
    <%= f.label :list %><br>
    <%= f.text_field :list, value: @task.list.name %>
  </div>

  <div class="field">
    <% supp = "supp".to_sym %>
    <%= f.label :list %><br>
    <%= f.text_field :list, value: @task.list.name %>
  </div>
  
  <div class="field">
    <%= f.label :tag %><br>
    <%= f.text_field :tag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
